const Frontpage = () => {
    // BACKGROUND COLOR
    if ((window.onload) = (e) => {
        document.body.style.backgroundImage =  "linear-gradient(white, #b1d2e8)"; 
    }) 
    
    if ((window.onclick) = (e) => {
        document.body.style.backgroundImage =  "linear-gradient(white, #b1d2e8)"; 
    })
    
    return (
        <>
        </>
    )
}

export default Frontpage; 